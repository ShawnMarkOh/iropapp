# Admin credentials for the IROPS Dashboard
# Set these to your desired admin username and password.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_super_secret_password

# A secret key for signing session cookies.
# Generate a new one for production with: python -c 'import os; print(os.urandom(24).hex())'
SECRET_KEY=a_very_long_and_random_secret_key_for_development
